# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/it_assets_manager

# Session Secret (Generate with: openssl rand -base64 32)
SESSION_SECRET=your-random-secret-key-here-change-in-production

# Environment
NODE_ENV=development

# App Configuration (for production deployment)
# NEXT_PUBLIC_APP_URL=http://your-server-ip/it-assets-manager

# Email Configuration
# Note: Email settings (SMTP/API) are configured through the Settings UI
# No environment variables needed for email functionality
